事件驱动模型：
    与观察者模式相近，可以用来观察者模式，但较为麻烦。而观察者模式实现事件驱动模型较为困难。
    个人认为两者之间的区别：
        观察者模式是被观察者发生变化，然后将变化推送给观察者们，即发布（release）--订阅（subscibe），变化（change）--更新（update）
        事件驱动模型是“观察者”一直“盯”着“被观察者”，只要被观察者发生变化，观察者就可立即注意到，并实施相应的策略...，即请求（request）--响应（response），事件发生（occur）--事件处理（handle）