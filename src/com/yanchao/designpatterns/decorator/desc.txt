装饰模式说明：
1.涉及到的类对象：抽象构件（顶级父类），具体构件（被装饰类），装饰角色（装饰类父类，不一定为抽象类），具体装饰角色（具体装饰类，可拥有多个）
2.装饰模式涉及到两类对象，一个是被装饰类对象（原生对象），一个是装饰类对象，装饰类对象的父类为抽象类，供不同形态的具体装饰类继承对被装饰对象进行装饰工作；
3.被装饰对象与装饰对象继承/实现于同一个父类/接口，具体的装饰类重写父类的方法时，通过多态方式（super）调用被装饰类的方法；